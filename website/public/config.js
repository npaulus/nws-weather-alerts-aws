window.API_CONFIG = {
  // For local development:
  baseUrl: 'http://localhost:3000'
  // For production (after SAM deploy):
  // baseUrl: 'https://<API GATEWAY>.execute-api.us-east-1.amazonaws.com/Prod'
};